# from flask import Blueprint, jsonify, redirect, url_for, session, request
# from app.models import SampleModel

# projects_routes = Blueprint('sample_pefix', __name__)


# @projects_routes.route('/sample')
# def get_sample():
    # sample_data = SampleModel.query.all()
    # data = [sample_data.to_dict() for samle in sample_data]
    # return {"sample": data}